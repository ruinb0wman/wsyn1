/**
 * WSync 配置文件模板
 * 放置于项目根目录，命名为 .wsync.config.js
 */

module.exports = {
  // ==========================================
  // 路径配置 (必需)
  // ==========================================

  // 源目录: WSL2 中的项目路径
  // 使用相对路径时，基于配置文件所在目录
  source: '.',

  // 目标目录: Windows 路径 (必需)
  // 示例: '/mnt/c/Users/你的用户名/Projects/项目名'
  // 工具在 WSL2 中运行，统一使用 /mnt/c/... 格式
  target: '{{TARGET}}',

  // ==========================================
  // 高级路径配置 (可选)
  // ==========================================

  // prefix: 文件同步到目标目录的哪个子文件夹下
  // 例如: prefix: 'src' 会将文件同步到 target/src/ 下
  // 留空则直接同步到 target 根目录
  prefix: '{{PREFIX}}',

  // ==========================================
  // 过滤规则
  // ==========================================

  // 白名单: 即使被 .gitignore 忽略，也强制同步
  // 支持 glob 模式: 'dir/', '*.ext', 'path/to/file'
  whitelist: [
    {{WHITELIST}}
  ],

  // 黑名单: 即使 .gitignore 没忽略，也强制排除
  blacklist: [
    {{BLACKLIST}}
  ],

  // 额外要遵循的 ignore 文件路径 (相对于 source)
  additionalIgnoreFiles: [
    // '.dockerignore'
  ],

  // ==========================================
  // 同步选项
  // ==========================================

  options: {
    // 是否删除目标端存在但源端不存在的文件
    delete: {{DELETE}},

    // 试运行: 只显示会做什么，不实际执行
    dryRun: false,

    // 详细日志: 显示每个文件的操作
    verbose: false
  }
};
